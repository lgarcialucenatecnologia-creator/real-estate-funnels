{
  "extends": "./tsconfig.json",
  // `scripts` queda fuera del bundle del servidor: son utilidades de
  // mantenimiento que se corren a mano con ts-node, no parte del API.
  "exclude": ["node_modules", "test", "dist", "scripts", "**/*spec.ts"]
}
